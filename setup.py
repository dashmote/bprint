import setuptools# import setup
setuptools.setup(
    name = 'bprint',
    version = '0.0.1',
    description = 'This package allows better printing',
    author = 'Kaifan Wei',
    # package_dir = {'': 'bprint'},
    packages = setuptools.find_packages(),
)
